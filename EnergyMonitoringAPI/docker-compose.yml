version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5066:80"
      - "7066:443"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Data Source=(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=oracle.fiap.com.br)(PORT=1521))(CONNECT_DATA=(SID=ORCL)));User Id=RM560139;Password=130399;
      - JwtSettings__SecretKey=sua-chave-super-secreta-com-pelo-menos-32-caracteres-aqui-fiap-2024
      - JwtSettings__Issuer=EnergyMonitoringAPI
      - JwtSettings__Audience=EnergyMonitoringAPIUsers
      - JwtSettings__ExpirationMinutes=60
    networks:
      - energy-network

networks:
  energy-network:
    driver: bridge